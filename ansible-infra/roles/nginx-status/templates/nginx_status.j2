# Rendered by Ansible for {{ inventory_hostname }}
#

server {
  listen {{ nginx_status_port }};
  rewrite .* /nginx_status last;
  location /nginx_status {
    stub_status on;
    access_log   off;
    allow 127.0.0.1;
    deny all;
  }
}
